{{#each results}}
<div class="album">
  <img src="{{ images.[1].url }}" />
  <div>
    <h2>{{ name }}</h2>
    <small>{{ genres }}</small>
    <br>
    <small>{{ description }}</small>
    <a href="/albums/">View Albums</a>
  </div>
</div>
{{/each}}